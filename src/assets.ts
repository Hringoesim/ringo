// Static asset URLs. Using BASE_URL keeps them correct whether the app is
// served from the domain root (Vercel) or a sub-path (GitHub Pages: /ringo/).
export const LOGO_SRC = import.meta.env.BASE_URL + 'ringo-logo.png';
